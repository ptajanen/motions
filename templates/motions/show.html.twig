{% extends 'base.html.twig' %} {% block title %}List of introduced motions{% endblock %} {% block body %}

<h1>Motion details</h1>
<table class="table table-hover">
    <thead>
        <tr>
            <th>ID</th>
            <th>Title</th>
            <th>Description</th>
            <th>Create Date</th>
            <th>Author</th>
            <th>Email</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <th>{{ motions.id }}</th>
            <td>{{ motions.title }}</td>
            <td>{{ motions.descrpt }}</td>
            <td>{{ motions.name }}</td>
            <td>{{ motions.email }}</td>

            <td>
                <a href="#" class="btn btn-success">Modify</a>
                <button delete-link = "{{motions.id }} class="btn btn-danger">Delete</button>
            </td>
        </tr>
    </tbody>
</table>

{% endblock %} 